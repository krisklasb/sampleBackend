spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          #issuer-uri: https://sts.windows.net/{TENANT_ID}/
          #audiences: ${CLIENT_ID}
          #jwk-set-uri: https://login.microsoftonline.com/common/discovery/keys //Höchst wahrscheinlich muss die jwk uri nicht angegeben werden
